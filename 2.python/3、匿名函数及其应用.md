### lambda

> lambda 参数1, 参数2:返回值
>
> 简化函数的书写形式，适用于短小的函数

### Map

> 会根据提供的函数对指定的序列做映射
>
> map函数的第一个参数是一个函数，如果函数的实现比较简单时，可以使用lambda表达式

![1606394235189](C:\Users\Administrator.JAMES\AppData\Roaming\Typora\typora-user-images\1606394235189.png)

```python 
def map_test(keys:list, values:list) --> Iterator:
    return map(lambda key, value:{key:value}, keys, values)


```

### reduce

### filter

### zip

### 递归

### 闭包